# Migrated to modular files:
#   settings.nix  - input, general, misc, dwindle, decoration, animations
#   binds.nix     - bind, bindm, bindl
#   exec-once.nix - autostart commands
#   monitors.nix  - monitor layout (extraConfig)
#   windowrules.nix - windowrule, layerrule, workspace rules
{ ... }: { }
